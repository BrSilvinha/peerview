# Server
PORT=3001

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=change-me-to-a-long-random-secret

# Admin credentials (password must be bcrypt-hashed in production)
ADMIN_EMAIL=admin@peerview.local
ADMIN_PASSWORD=changeme123

# Frontend origin (used for CORS and session link generation)
FRONTEND_URL=http://localhost:3000
